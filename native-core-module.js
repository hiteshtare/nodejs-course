//Node module (core/native)
var util = require('util');

var name = "Hitesh";

var demo = util.format('Hello %s', name);//String formatting utility
util.log(demo);//Similar to console.log (timestamp additional)